# resiliencx 上下文

本文件定义 `resiliencx` 与其使用方共享的核心词汇。它只记录领域含义与能力边界，
不记录具体实现、API 签名、存储或部署决定。

## 角色与边界

**弹性原语**：可在进程内直接调用的可靠性积木（重试、预算、熔断、限流、舱壁）；
它不编排调用方业务、不做跨进程协调，也不撤销已经发生的外部副作用。
_Avoid_: 弹性平台（本 crate 没有控制面、策略下发与全局配额）

**无墙钟**：原语的状态推进只依赖调用方显式传入的输入与计数，不把读取系统时间当作正确性条件。
_Avoid_: 定时器 / 自动冷却（熔断与限流不会随时间自行恢复，恢复由显式调用驱动）

**本地原语**：只维护当前进程内状态的保护设施，实例之间不共享、不协商、不汇总。
_Avoid_: 分布式限流 / 集群熔断（跨进程协调明确超出本 crate 边界）

**观测注入**（`Instrumentation`）：来自共享契约 `instrumentationx` 的事件出口，
只登记重试次数与熔断开合；实现细节（如 `tracing` 后端）不属于本 crate。
_Avoid_: 直接依赖 `observex`（会把具体观测实现拖进本 crate 依赖图）

## 重试语义

**副作用声明**（`RetrySafety`）：调用方对操作能否安全重复执行的显式声明，取值为
`ReadOnly` / `Idempotent` / `UnsafeSideEffect`；它**不是**对闭包内容的静态证明，
也不替代幂等键、事务或补偿机制。
_Avoid_: 幂等保证（声明由调用方负责，原语无法验证其真伪）

**安全入口**：带 `_safe` 后缀、会在首次调用前校验副作用声明的重试入口；`max_attempts > 1`
时拒绝 `UnsafeSideEffect`。
_Avoid_: 新接口（兼容面并未废弃，只是不执行该校验）

**Unchecked 兼容面**：不带 `_safe` 的同类入口（`retry_fn` / `retry_async` 等），
语义完整但不替调用方校验副作用安全性；不得被描述为生产安全路径。
_Avoid_: 内部接口（它们仍是公开 API，只是责任划分不同）

**退避与抖动**（`Backoff` / `jitter`）：失败后下一次尝试前的等待策略与延迟收缩；
抖动由调用方 seed 驱动，是确定性伪随机，**不是**加密 RNG。
_Avoid_: 随机退避（无 seed 的入口只依赖 attempt，多实例同相、不抗群聚）

**重试尝试序号**（`attempt`）：刚失败、且即将触发下一次 retry 的尝试编号，从 1 起；
预算耗尽时不产生该事件。
_Avoid_: 成功计数（它不表示已完成的调用总数）

## 容量与状态

**重试预算**（`RetryBudget`）：一段逻辑窗口内允许消耗的重试令牌计数，约束的是「再试几次」；
它不感知请求总量与业务重要性。async 路径以预留 + RAII 归还实现，取消不泄漏令牌。
_Avoid_: 限流（限流约束调用速率，预算约束失败后的重试次数）

**熔断三态**（`CircuitState`）：`Closed` 放行、`Open` 短路拒绝、`HalfOpen` 试探；
`Open → HalfOpen` 由累计拒绝次数推进，而非等待时长。
_Avoid_: 熔断超时（本 crate 没有冷却计时器）

**令牌桶限流**（`RateLimiter`）：按可用令牌决定放行或立即 `Unavailable`；令牌只由
显式 `refill` 补充，没有自动按时间回填。
_Avoid_: 排队 / 公平调度（令牌不足时立即拒绝，不排队、不给 deadline）

**舱壁**（`Bulkhead` / `BulkheadPermit`）：限制同时进入保护段的调用数，许可用 RAII 归还
（含 panic unwind）；满载立即拒绝，没有排队等待。
_Avoid_: 连接池（舱壁限制并发段数，不管理可复用连接对象）

## 错误与取消

**语义分类**（`ErrorKind`）：按「调用方应如何反应」划分的错误类别（`Invalid` / `Transient` /
`Unavailable` / `Invariant` 等）；禁止用字符串匹配或类型断言替代对它的匹配。
_Avoid_: 错误码（分类表达的是处置语义，不是来源系统的编号）

**可重试**：`ResiliencxError::is_retryable()` 为真的性质，**仅** `Transient` 具备；
其余分类立即原样返回，不会退避重试。
_Avoid_: 网络错误（网络只是成因之一，不能表达「重复施加是否安全」）

**deadline**：覆盖整次重试（含每次 operation future 与退避等待）的截止时间，
超时统一映射为 `DeadlineExceeded`；它不替代幂等键或补偿。
_Avoid_: 单次超时（deadline 约束的是整轮重试，不是单次请求）

**协作式取消**（cooperative cancellation）：超时后停止轮询未完成的 future；
已经完成的网络写入或数据库提交**不会**被撤销，operation 自派生的后台任务也可能继续运行。
_Avoid_: 回滚（本 crate 不提供副作用的自动撤销）
